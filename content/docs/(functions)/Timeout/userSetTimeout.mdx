---
title: "$userSetTimeout"
---

Sets a user timeout, so the user cannot talk/interact in the server.

## Usage

```php
$userSetTimeout[user id;time (optional, default:'10m');reason (optional)]
```

