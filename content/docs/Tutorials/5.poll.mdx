---
title: "Sending Polls"
---

In this guide, you will learn how to send a poll.

## Creating Command

First step is to [create a command](/Guide/1.create).

## Trigger Options

Let assume the user wants to say `!poll` to send the poll:

![](/images/tutorials/poll/0.png)

## Code

For the code, we will use:
- `$sendMessage`
- [poll curl format](/Other/curl#polls)

## Constructing the Poll

Now we need to create a simple poll:

```cc
{poll:
    {question=Do you like turtles?}
    {duration=2h}
    
    {answer=Yes}
    {emoji=🐢}
    
    {answer=No}
    {emoji=❌}
}
```

And put it inside of `$sendMessage` to send it:

```cc
$sendMessage[
    {poll:
        {question=Do you like turtles?}
        {duration=2h}

        {answer=Yes}
        {emoji=🐢}

        {answer=No}
        {emoji=❌}
    }
]
```

After the code is done, don't forget to save the command.

## Testing

Send the command and see if it works!

<Discord>
    <Message>
        !poll
    </Message>
    <Message bot>
        <Poll question="Do you like turtles?" time="120m">
            <Answer text="Yes" emoji="🐢"/>
            <Answer text="No" emoji="❌"/>
        </Poll>
    </Message>
</Discord>
