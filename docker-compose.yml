services:
  ccdoc-dev:
    build:
      context: .
      dockerfile: DockerfileDev
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - 8080:8080
    command: pnpm dev
    environment:
      - NODE_ENV=development
